# Copy to keystore.properties (gitignored) for local release signing.
# CI uses GitHub Actions secrets instead — see README.md.

storeFile=release.keystore
storePassword=changeit
keyAlias=pgp-shield
keyPassword=changeit
